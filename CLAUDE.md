# Project

## Overview
<!-- プロジェクトの目的・概要をここに記述 -->

## Development Commands
<!-- よく使うコマンドを追記してください -->
<!-- 例:
- `npm install` — 依存関係インストール
- `npm run dev`  — 開発サーバー起動
- `npm test`     — テスト実行
- `npm run lint` — lint実行
-->

## Architecture
<!-- アーキテクチャ・主要ファイル構成を記述 -->

## Code Conventions
- コミットメッセージは日本語OK
- 変更前に必ず該当ファイルを Read すること
- 不要なファイルを新規作成しない（既存ファイルを優先）
- セキュリティ: 秘密情報をコードにハードコードしない

## Important Notes
- `.env` ファイルは絶対にコミットしない
- 破壊的操作（rm -rf, force push等）は必ずユーザー確認を取る
